name: portfolio_flutter
description: "A personal portfolio ledger backed by Google Sheets."
publish_to: 'none'

version: 0.1.11+12

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_sign_in: ^6.2.2
  flutter_riverpod: ^2.6.1
  uuid: ^4.5.1
  url_launcher: ^6.3.1
  http: ^1.2.2
  crypto: ^3.0.7
  web: ^1.1.1
  shared_preferences: ^2.3.4
  intl: ^0.19.0
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  package_info_plus: ^9.0.0
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
